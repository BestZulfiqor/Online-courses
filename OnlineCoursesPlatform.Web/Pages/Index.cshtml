@page
@model IndexModel
@{
    ViewData["Title"] = "Главная";
}

<div class="container">
    <!-- Hero Section -->
    <div class="row py-5 text-center">
        <div class="col-lg-8 mx-auto">
            <h1 class="display-4 fw-bold">Добро пожаловать в Онлайн Курсы</h1>
            <p class="lead mb-4">Получите доступ к качественному образованию от лучших преподавателей. Развивайте свои навыки и достигайте новых высот в карьере.</p>
            <div class="d-grid gap-2 d-sm-flex justify-content-sm-center">
                <a href="/Courses" class="btn btn-primary btn-lg px-4 gap-3">Просмотреть курсы</a>
                <a href="/Account/Register" class="btn btn-outline-secondary btn-lg px-4">Регистрация</a>
            </div>
        </div>
    </div>

    <!-- Features Section -->
    <div class="row g-4 py-5">
        <div class="col-lg-4">
            <div class="feature-icon bg-primary bg-gradient p-3 rounded-3 text-white mb-3">
                <i class="bi bi-book-half fs-2"></i>
            </div>
            <h2>Качественное обучение</h2>
            <p>Наши курсы разработаны профессионалами своего дела и постоянно обновляются.</p>
        </div>
        <div class="col-lg-4">
            <div class="feature-icon bg-primary bg-gradient p-3 rounded-3 text-white mb-3">
                <i class="bi bi-people fs-2"></i>
            </div>
            <h2>Опытные преподаватели</h2>
            <p>Учитесь у лучших специалистов с богатым практическим опытом.</p>
        </div>
        <div class="col-lg-4">
            <div class="feature-icon bg-primary bg-gradient p-3 rounded-3 text-white mb-3">
                <i class="bi bi-clock fs-2"></i>
            </div>
            <h2>Гибкий график</h2>
            <p>Учитесь в удобное для вас время и в своем темпе.</p>
        </div>
    </div>

    <!-- Popular Courses Section -->
    <div class="row py-5">
        <h2 class="text-center mb-4">Популярные курсы</h2>
        @foreach (var course in Model.PopularCourses)
        {
            <div class="col-md-4 mb-4">
                <div class="card h-100">
                    <img src="@course.ImageUrl" class="card-img-top" alt="@course.Title">
                    <div class="card-body">
                        <h5 class="card-title">@course.Title</h5>
                        <p class="card-text">@course.Description</p>
                        <div class="d-flex justify-content-between align-items-center">
                            <span class="h5 mb-0">@course.Price.ToString("C0")</span>
                            <a href="/Courses/Details/@course.Id" class="btn btn-primary">Подробнее</a>
                        </div>
                    </div>
                </div>
            </div>
        }
    </div>

    <!-- Statistics Section -->
    <div class="row py-5 text-center bg-light rounded-3 mb-5">
        <div class="col-md-4">
            <h2 class="display-4 fw-bold">@Model.TotalStudents</h2>
            <p>Студентов</p>
        </div>
        <div class="col-md-4">
            <h2 class="display-4 fw-bold">@Model.TotalCourses</h2>
            <p>Курсов</p>
        </div>
        <div class="col-md-4">
            <h2 class="display-4 fw-bold">@Model.TotalInstructors</h2>
            <p>Преподавателей</p>
        </div>
    </div>
</div>

@section Styles {
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.7.2/font/bootstrap-icons.css">
    <style>
        .feature-icon {
            width: 4rem;
            height: 4rem;
            display: flex;
            align-items: center;
            justify-content: center;
        }
    </style>
}
